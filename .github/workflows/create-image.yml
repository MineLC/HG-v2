name: Build and Push Docker Image

on:
  push:
    branches:
      - '*'

jobs:
  build_and_push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify GitHub token
        run: |
          if [[ $(curl -s -o /dev/null -w "%{http_code}" -H "Authorization: token $GITHUB_TOKEN" https://api.github.com/repos/$GITHUB_REPOSITORY) == 200 ]]; then
            echo "El token GH_TOKEN es válido."
          else
            echo "El token GH_TOKEN no es válido."
            exit 1
          fi

      - name: Set build number
        id: version
        run: echo "::set-output name=BUILD_VERSION::v0.${{ github.run_number }}"

      - name: Generate Dockerfile and Entrypoint
        run: |
          cat > Dockerfile <<EOF
          # Use a base image
          FROM azul/prime:17

          # Install necessary packages
          ARG DEBIAN_FRONTEND=noninteractive
          ENV TZ=Etc/UTC
          RUN apt-get install -y tzdata
          RUN apt-get update -y && \
              apt-get install -y git maven && \
              rm -rf /var/lib/apt/lists/*

          # Clone and build the Spigot server
          ENV GITHUB_TOKEN=${GITHUB_TOKEN}
          RUN git config --global credential.helper '!f() { echo "username=ToshioDev"; echo "password=$GITHUB_TOKEN"; }; f'
          RUN git clone --branch minimal https://ToshioDev:${GITHUB_TOKEN}@github.com/MineLC/LCSpigot.git /tmp/spigot && \
          cd /tmp/spigot && \
          mvn clean package

          # Set up the Minecraft server directory
          WORKDIR /minecraft
          RUN cp /tmp/spigot/target/lcspigot-*.jar .

          # Clone and build additional plugins
          RUN git clone https://github.com/MineLC/LC-Commons.git /tmp/commons && \
              cd /tmp/commons && \
              mvn clean package && \
              cp target/LCCommons-1.0-SNAPSHOT.jar /tmp/spigot/plugins/

          RUN git clone https://github.com/MineLC/HG-v2.git /tmp/plugin && \
              cd /tmp/plugin && \
              mvn clean package && \
              cp target/hg-0.0.1-jar-with-dependencies.jar /tmp/spigot/plugins/

          RUN git clone https://github.com/MineLC/SlimeWorldManager.git /tmp/slime && \
              cd /tmp/slime && \
              mvn clean package && \
              cp target/slimeworldmanager-plugin-2.2.0-SNAPSHOT-jar-with-dependencies.jar /tmp/spigot/plugins/ && \
              cp target/slimeworldmanager-classmodifier-2.2.0-SNAPSHOT.jar /tmp/spigot/

          # Set default environment variables for min memory and memory
          ENV MINMEMORY=4608
          ENV MEMORY=4608

          # Set the default port to 25565
          ENV PORT=25565

          # Expose the port
          EXPOSE 25565

          # Set the command to run the server with the specified memory options
          CMD ["/entrypoint.sh"]
          EOF

          cat > entrypoint.sh <<'EOL'
          #!/bin/bash
          set -euo pipefail

          # Set up the memory options for Java
          JAVA_OPTS="-Xms${MINMEMORY}M -Xmx${MEMORY}M"

          # Run the Minecraft server with the specified memory options and custom port
          java $JAVA_OPTS -javaagent:slimeworldmanager-classmodifier-2.2.0-SNAPSHOT.jar -XX:+UseG1GC -XX:+ParallelRefProcEnabled -XX:MaxGCPauseMillis=200 -XX:+UnlockExperimentalVMOptions -XX:+DisableExplicitGC -XX:+AlwaysPreTouch -XX:G1HeapWastePercent=5 -XX:G1MixedGCCountTarget=4 -XX:InitiatingHeapOccupancyPercent=15 -XX:G1MixedGCLiveThresholdPercent=90 -XX:G1RSetUpdatingPauseTimePercent=5 -XX:SurvivorRatio=32 -XX:+PerfDisableSharedMem -XX:MaxTenuringThreshold=1 -Dusing.aikars.flags=https://mcflags.emc.gs -Daikars.new.flags=true -XX:G1NewSizePercent=30 -XX:G1MaxNewSizePercent=40 -XX:G1HeapRegionSize=8M -XX:G1ReservePercent=20 -jar lcspigot-*.jar --port $PORT
          EOL

      - name: Build Docker image
        run: docker build -t my-image .